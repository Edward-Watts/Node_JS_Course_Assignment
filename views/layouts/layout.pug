doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(link rel="stylesheet" href="/css/main.css")
        title #{pageTitle}
        block styles
    body 
        header.main-header
            nav.main-nav
                ul
                    li
                        a(class=(path === "/" ? "active" : ""), href="/") Users
                    li
                        a(class=(path === "/admin/add-users" ? "active" : ""), href="/admin/add-users") Add Users
        block content